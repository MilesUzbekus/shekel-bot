# --- ядро (данные + аналитика), работает без секретов ---
requests>=2.31

# --- телеграм-бот (подключается на деплое) ---
python-telegram-bot[job-queue]>=21.0

# --- чтение Google Sheet через service account ---
gspread>=6.0
google-auth>=2.28

# --- ИИ-прогноз: парсинг RSS-новостей (чистый python, переносимо) ---
feedparser>=6.0
